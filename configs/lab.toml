schema_version = "1.0"
mode = "fixture"
model_provider = "offline"
model_id = "deterministic-fixture-v1"
data_handling = "Local files only; the fixture adapter has no network or filesystem tools."

[budget]
max_elapsed_seconds = 30
max_model_calls = 0
max_tool_calls = 40
max_tokens = 0
max_repair_rounds = 0
max_cost_usd = "0"

